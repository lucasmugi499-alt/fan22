// The fan homepage builds tables across all public demo leagues. Keep this above the
// canonical demo fixture count so global Firestore reads do not zero out later leagues.
export const FAN_HOME_RECORD_LIMIT = 700;
